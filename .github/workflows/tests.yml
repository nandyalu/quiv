name: Tests

on:
  push:
    paths:
      - "quiv/**"
      - "tests/**"
      - "pyproject.toml"
  pull_request:
    paths:
      - "quiv/**"
      - "tests/**"
      - "pyproject.toml"
  workflow_dispatch:

permissions:
  pull-requests: write

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.11", "3.12", "3.13", "3.14"]
    steps:
      - uses: actions/checkout@v7

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: uv sync

      - name: Run tests with coverage
        run: uv run pytest tests/ -v --cov=quiv --cov-report=term --cov-report=xml

      - name: Upload coverage report
        # GITHUB_TOKEN is read-only for fork PRs, so the coverage comment can only be posted for same-repo PRs
        if: matrix.python-version == '3.13' && github.event_name == 'pull_request' && github.event.pull_request.head.repo.full_name == github.repository
        uses: orgoro/coverage@v3.3
        with:
          coverageFile: coverage.xml
          token: ${{ secrets.GITHUB_TOKEN }}
